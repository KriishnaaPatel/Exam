#include<stdio.h>
int main(){
	int count;
	int i;
	int n;
	
    do {
        if (i % 2 = 0) { 
            sum += i;
        }
        i++;
    } while (i <= n);

printf("Odd numbers are : %d",count);
	return 0;
}
